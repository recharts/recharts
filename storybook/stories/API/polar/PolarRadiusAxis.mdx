import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as ComponentStories from './PolarRadiusAxis.stories';

# PolarRadiusAxis

<Meta of={ComponentStories} />

<Canvas of={ComponentStories.API} layout="padded" />

## Parent Component

The PolarRadiusAxis can be used within the following parent components:

- `<PieChart/>`
- `<RadarChart/>`
- `<RadialBarChart/>`

## Child Components

The PolarRadiusAxis can be used with the following child components:

- `<Label/>`

## Props

<Controls of={ComponentStories.API} />
