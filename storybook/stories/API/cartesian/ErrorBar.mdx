import { Canvas, Meta, ArgTypes } from '@storybook/blocks';
import * as ErrorBarStories from './ErrorBar.stories';

# ErrorBar

## Parent Component

The ErrorBar can be used within `<Bar />`, `<Line />` or `<Scatter />`.

## Properties

Properties in the groups Other and Internal are not recommended to be used.

<ArgTypes of={ErrorBarStories} sort={'requiredFirst'} />
