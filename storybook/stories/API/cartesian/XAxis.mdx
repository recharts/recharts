import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as ComponentStories from './XAxis.stories';

# XAxis

<Meta of={ComponentStories} />

<Canvas of={ComponentStories.API} layout='padded'/>

## Parent Component

The XAxis can be used within the following parent components:
- `<AreaChart/>`
- `<BarChart/>`
- `<ComposedChart/>`
- `<FunnelChart/>`
- `<LineChart/>`
- `<ScatterChart/>`

## Child Components

The XAxis can be used with the following child components:
- `<Label/>`

## Props

<Controls of={ComponentStories.API} />
