import { Canvas, Meta, ArgTypes , Story, Title } from '@storybook/blocks';

import * as LineStories from './Line.stories';

# Line

## Parent Component
The Line can be used within a `<ComposedChart/>` or a `<LineChart/>`.

## Child component
The Line can be used with the following child components: `<ErrorBar/>` `<LabelList/>`


## Properties
<ArgTypes of={LineStories} sort={'requiredFirst'}/>






