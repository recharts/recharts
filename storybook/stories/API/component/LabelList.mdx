import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as ComponentStories from './LabelList.stories';

# LabelList

The LabelList component displays one label for each data point in a series.

<Meta of={ComponentStories} />

<Canvas of={ComponentStories.API} layout='padded' />

## Parent Components

The LabelList can be used within a `<Area />`, `<Bar />`, `<Line />`, `<Scatter />`, `<Pie />`, `<Radar />`, `<RadialBar />`, or `<Funnel />`

## Properties

<Controls of={ComponentStories.API} />
