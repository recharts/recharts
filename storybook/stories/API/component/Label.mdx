import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as ComponentStories from './Label.stories';

# Label

The Label component displays a single text label at a specified position in a chart or in a parent component.

<Meta of={ComponentStories} />

<Canvas of={ComponentStories.CartesianPositions} layout="padded" />

## Parent Components

The Label can be used within a `<XAxis />`, `<YAxis />`, `<ReferenceArea />`, `<ReferenceDot />`, `<ReferenceLine />`, `<Pie />` or a `<PolarRadiusAxis />`

## Properties

<Controls of={ComponentStories.CartesianPositions} />
