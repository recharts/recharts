import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as ComponentStories from './Treemap.stories';

# Treemap

<Meta of={ComponentStories} />

<Canvas of={ComponentStories.API} layout='padded'/>

## Description

<p>The Treemap chart is used to visualize hierarchical data using nested rectangles.</p>


## Parent Component

The Treemap can be used within the following parent components:
- `<ResponsiveContainer/>`

## Child Components

The Treemap can be used with the following child components:
- `<Tooltip/>`

## Props

<Controls of={ComponentStories.API} />
